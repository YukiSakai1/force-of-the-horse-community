# サイト更新ガイド（非エンジニアの方向け）

このサイトは、以下の4種類の更新に対応しています。**「ガイドラインの文言修正」以外は、すべてGoogleスプレッドシートの操作だけで完結します。HTMLファイルを開く必要はありません。**

操作するスプレッドシートは、フォームの送信データが自動で溜まっているあのシートです（`Code.gs`をApps Scriptに設定した際に作ったものです）。

---

## 1. FAQの回答を追加する

シート名: **「FAQ質問」**

| 列名 | 入力する内容 |
|---|---|
| ステータス | `回答済み` と入力（半角・全角どちらでもOKですが、必ず「回答済み」という文字と完全に一致させてください） |
| 回答 | 回答内容をそのまま入力 |

**注意点:**
- 「ステータス」が「回答済み」に**なっていて、かつ「回答」に何か文字が入っている行だけ**が公開されます。片方だけでは表示されません。
- 「対象弾」の列は、フォーム送信時の選択肢（例:「ブースターパックVol.01 『競馬、新章』」）がそのまま入っています。ここは変更しないでください（自動判定に使われています）。
- 公開されると、対応するFAQページ（例:「対象弾」が Vol.01 なら`faq-vol1.html`）に自動的に表示されます。反映まではページを再読み込みすれば確認できます（サイト側の再デプロイ等は不要です）。

---

## 2. お知らせを追加する

シート名: **「お知らせ」**（もし無ければ、下記の見出しで新しいシートを1つ作ってください）

| 列名 | 入力する内容 |
|---|---|
| 掲載日 | 例: `2025/8/1` |
| タグ | `お知らせ` / `イベント` / `FAQ` のいずれか（自由入力ですが、この3つ以外は「お知らせ」タグと同じ見た目になります） |
| タイトル | お知らせのタイトル |
| 本文 | お知らせの本文（1〜2文程度がおすすめです） |
| 公開 | `公開` と入力（この文字が入っていない行はトップページに表示されません） |

**注意点:**
- 新しい行を一番上に追加しても、一番下に追加しても構いません。日付が新しい順に自動で並び替えて表示されます。
- 最大10件まで表示されます。

---

## 3. 大会・イベント情報を公開する

シート名: **「申請」**（`apply.html`から送られてくる大会申請フォームのデータです）

| 列名 | 入力する内容 |
|---|---|
| ステータス | 内容を確認してOKなら `承認済み` と入力 |

**注意点:**
- 「ステータス」を「承認済み」に変えるだけで、イベントカレンダーページとトップページのミニカレンダーの両方に自動反映されます。
- 却下する場合は「却下」など、「承認済み」以外の文字にしておけば、公開されません（削除する必要はありません）。
- カレンダー上の色分けは、「イベント種別」が「公認大会」なら青系、それ以外（交流会・初心者体験会・観戦会・配信イベント）は緑系の色になります。

---

## 4. ガイドラインページの文言を修正する（ここだけHTML直接編集）

対象ファイル: **`guideline.html`**

この作業だけは、テキストエディタで直接ファイルを開いて修正します。安全に編集するためのルールは次の1つだけです。

> **`<` と `>` で囲まれた部分（タグ）には絶対に触らない。その間にある日本語の文章だけを書き換える。**

例えば、下記のような行があったとします。

```html
<p>プレイヤーの皆さまによる自主的なイベント開催・コミュニティ活動を支援するため、公認イベント制度を設けています。</p>
```

この場合、`<p>` と `</p>` の**外側は絶対に消さず**、間にある文章だけを書き換えればOKです。

```html
<p>ここを新しい文章に書き換えればOKです。</p>
```

**やってはいけないこと:**
- `<p>` や `</p>`、`<li>`や`</li>`のようなタグ自体を消す・追加する
- `"` (ダブルクォーテーション) を文章の中に直接使う（代わりに `&quot;` を使うか、` ' `（シングルクォーテーション）を使ってください）
- タグの数を変える（新しい項目を丸ごと追加したい場合は、まず私に相談してください。テンプレートを用意します）

修正したら、ブラウザで`guideline.html`を開いて見た目が崩れていないか確認してから、サーバーにアップロードしてください。

---

## 5. NGワード（不適切な投稿を弾く言葉）を追加する

大会申請・FAQ質問の自由記述欄には、あらかじめ運営側で設定した「NGワード」が含まれていると、
送信自体がブロックされる仕組みが入っています（メールアドレス・URLの形式チェック、URLを
大量に含む投稿のブロックもあわせて自動で行われます）。

デフォルトのNGワードに加えて、単語を追加したい場合はHTMLではなく、Apps Script側の設定で行います。

1. スプレッドシートのメニューから **拡張機能 → Apps Script** を開く
2. 左メニューの「プロジェクトの設定」→「スクリプトプロパティ」を開く
3. プロパティ名 `NG_WORDS_EXTRA` を追加し、値に**カンマ区切り**で単語を入力する
   例: `出会い系,アダルト,詐欺まがい`
4. 保存すれば、次の送信からすぐに反映されます（サイト側の再デプロイは不要です）

**注意点:**
- あくまで機械的な単語一致による簡易フィルタなので、100%の荒らし対策にはなりません。ブロックされなかった投稿については、これまで通りスプレッドシート上で内容を確認してから公開してください。
- 単語を追加しすぎると、正当な投稿まで誤ってブロックしてしまう可能性があります（例えば商品名やカード名の一部と偶然一致する等）。心配な場合はご相談ください。

---

## 困ったときは

- スプレッドシートに入力したのに反映されない → 「ステータス」や「公開」の文字が完全に一致しているか確認してください（余分なスペースが入っていないか等）
- 大会情報やお知らせの並び順がおかしい → 日付の入力形式を確認してください
- HTML編集後に見た目が崩れた → 編集前の内容に戻すか、そのままのファイルを送ってもらえれば直します
