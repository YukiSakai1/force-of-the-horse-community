# Force of the Horse サイト公開ガイド

## このアップデートで変わったこと

- `apply.html`（イベント申請フォーム）と `faq.html`（FAQ質問投稿）、および各Vol別ページ
  （`faq-starter.html` / `faq-vol1〜5.html`）下部の質問フォームが、実際にデータを送信するようになりました。
- 送信先は Google スプレッドシート（Google Apps Script 経由）です。
- 送信があると（設定すれば）Slackに通知が飛びます。
- `apply.html` の各入力欄に `name` 属性を追加しています（データ収集のため。見た目は変わりません）。

**注意**：`CONFIG.GAS_WEB_APP_URL` を設定するまでは、フォームを送信するとエラーメッセージが出ます（「送信に失敗しました」）。下記の手順で設定してください。

---

## 手順1：スプレッドシート＋Apps Scriptを作る（15分程度）

1. Google スプレッドシートを新規作成（例：「FOTH運営データ」という名前で保存）
2. メニューの **拡張機能 → Apps Script** を開く
3. デフォルトで開いている `Code.gs` の中身を全部消して、`apps-script/Code.gs` の内容を貼り付ける
4. 上部の「保存」（フロッピーアイコン）をクリック
5. **実行**ボタンを一度押して、権限の許可を求められたら許可する（`doGet`関数などが選ばれていればOK。エラーが出ても問題ありません、権限確認が目的です）

### Slack通知を使う場合（任意）
1. SlackでIncoming Webhookを作成し、Webhook URLを取得
2. Apps Scriptエディタの左メニュー「プロジェクトの設定」→「スクリプトプロパティ」
3. プロパティ名 `SLACK_WEBHOOK_URL`、値にWebhook URLを設定して保存

### メール通知を使う場合（任意、Slackと併用可）
1. Apps Scriptエディタの左メニュー「プロジェクトの設定」→「スクリプトプロパティ」
2. プロパティ名 `NOTIFY_EMAIL`、値に通知を受け取りたいメールアドレスを設定して保存
   （複数人に送りたい場合はカンマ区切りで複数アドレスを入力: `a@example.com,b@example.com`）
3. 初回のフォーム送信時に、Googleアカウントから「メール送信の権限」を確認するポップアップが
   出ることがあります。その場合は許可してください
4. 送信元は、スプレッドシートを作成したGoogleアカウント（＝Apps Scriptを実行しているアカウント）になります
5. 無料のGoogleアカウントの場合、1日に送れるメール数に上限があります（通常1日100通程度）が、
   通常の申請・質問件数であれば問題になることはまずありません

### reCAPTCHA v3（ボット・スパム対策、任意だが推奨）
すでにサイト側（`script.js`）・受信側（`Code.gs`）ともに対応コードは入っているので、
以下の2つのキーを取得して設定するだけで有効になります（設定するまでは、これまで通り
reCAPTCHAなしで動作します）。

1. [https://www.google.com/recaptcha/admin](https://www.google.com/recaptcha/admin) にアクセスし、Googleアカウントでログイン
2. 新しいサイトを登録：
   - ラベル: 任意（例: `Force of the Horse`）
   - reCAPTCHAタイプ: **reCAPTCHA v3** を選択（v2ではないので注意）
   - ドメイン: 公開するドメイン（例: `force-of-the-horse.com`）を追加。まだ未公開でもドメイン名さえ決まっていれば先に登録可能です。ローカル確認用に `localhost` も追加しておくと便利です
   - 利用規約に同意して送信
3. 発行される2つのキーをそれぞれ設定します：
   - **サイトキー**（公開してよい方） → `script.js` 冒頭の `CONFIG.RECAPTCHA_SITE_KEY` を書き換える
     ```js
     RECAPTCHA_SITE_KEY: 'ここにサイトキーを貼り付け'
     ```
   - **シークレットキー**（絶対に公開しない方） → Apps Scriptエディタ左メニュー「プロジェクトの設定」→
     「スクリプトプロパティ」→ プロパティ名 `RECAPTCHA_SECRET_KEY` に値として貼り付けて保存
4. 両方設定できたら、実際に各フォーム（`apply.html` / `faq.html` / 各Volページ）から試しに送信し、
   スプレッドシートに反映されるか確認してください
5. 画面右下に小さくGoogleのreCAPTCHAバッジが表示されるようになりますが、これは正常な状態です
   （バッジの表示はreCAPTCHAの利用規約で必須とされています）

**注意点:**
- 2つのキーは必ずセットで設定してください。サイトキーだけ設定してシークレットキーが未設定の間は、
  reCAPTCHAの判定自体は行われません
- 判定スコアが低い（ボットらしい）と送信自体が「送信に失敗しました」というエラーになります。
  実際の利用者が誤って弾かれるケースが多い場合は、`Code.gs`内の`RECAPTCHA_SCORE_THRESHOLD`
  （初期値0.5）を少し下げると通りやすくなります

### 送信頻度の制限（レート制限）
こちらは追加の設定作業なしで、`Code.gs`をアップロードするだけで自動的に有効になります。

- 同じブラウザ（`script.js`が発行するランダムなclientIdで識別）からの送信は、**1分間に3件まで**
- サイト全体では、**1分間に30件まで**

を超えると、それ以降の送信は一時的に拒否され、フォーム側には「送信が集中しています」という
専用のメッセージが表示されます（見た目は失敗扱いですが、実際にはスプレッドシートに変な行が
増えるのを防ぐための正常な動作です）。

Google Apps Scriptの仕様上、送信元のIPアドレスそのものは取得できないため、ブラウザ単位の
識別（clientId、localStorageに保存）による制限になります。ブラウザのlocalStorageを消す・
別ブラウザやシークレットモードを使う、といった方法で個別の制限は回避されえますが、その場合も
サイト全体の上限（1分間30件）がもう一段の歯止めとして働きます。

制限の数値を変えたい場合は、`Code.gs`内の`RATE_LIMIT_PER_CLIENT`・`RATE_LIMIT_GLOBAL`を
書き換えてください。

### デプロイ
1. 右上の「デプロイ」→「新しいデプロイ」
2. 種類の選択（歯車アイコン）→「ウェブアプリ」
3. 設定：
   - 実行するユーザー：**自分**
   - アクセスできるユーザー：**全員**
4. 「デプロイ」をクリックし、発行された **ウェブアプリURL** をコピー
   （`https://script.google.com/macros/s/xxxxx/exec` の形式）

これで送信するとスプレッドシートに自動で「申請」シートと「FAQ質問」シートが作られ、行が追加されていきます。

---

## 手順2：サイト側にURLを設定する

`script.js` の冒頭付近、以下の箇所を書き換えます。

```js
const CONFIG = {
    GAS_WEB_APP_URL: 'YOUR_GAS_WEB_APP_URL_HERE'  // ← ここを手順1で取得したURLに置き換える
};
```

置き換えたら、ローカルで `index.html` をブラウザで開いてapply.htmlやfaq.htmlから試しに送信し、スプレッドシートに反映されるか確認してください。

---

## 手順3：ドメイン取得＆公開

### おすすめ構成
静的サイトホスティング（**Cloudflare Pages** または **Netlify**）＋独自ドメイン。どちらも無料枠でこの規模のサイトなら十分です。

### 手順
1. **GitHubリポジトリを作る**：このフォルダ一式（apps-scriptフォルダは除く）をpush
2. **ホスティングに接続**：
   - Cloudflare Pages の場合：ダッシュボードで「Pages」→「Gitに接続」→リポジトリ選択→ビルド設定なし（静的HTMLなので「ビルドコマンドなし」でOK）
   - Netlify の場合も同様にリポジトリ連携でOK
3. デプロイすると `xxxx.pages.dev`（Cloudflareの場合）のようなURLでまず公開される
4. **ドメイン取得**：お名前.com、Cloudflare Registrar などで取得（`.com`なら年1,000〜1,500円程度）
5. **ドメイン接続**：
   - Cloudflareでドメインも管理している場合：Pagesの「カスタムドメイン」設定にドメインを追加するだけでDNS・SSLが自動設定される
   - 他社レジストラの場合：ホスティング側の指示に従いCNAME/AレコードをDNS設定に追加
6. SSL証明書は上記のホスティングなら自動発行されるので追加作業は不要

### 公開後の運用フロー（非エンジニアの方向け）
1. ユーザーが申請・質問を送信
2. スプレッドシートに自動保存＋Slack通知（設定していれば）
3. 運営（Yukiさん）がスプレッドシートを確認する
4. **FAQ**：「FAQ質問」シートの「回答」列に回答を書き、「ステータス」列を「回答済み」に変更する → 該当する`faq-starter.html`/`faq-vol1〜4.html`に自動で表示される（HTMLを直接編集する必要はありません）
5. **大会・イベント情報**：「申請」シートの「ステータス」列を「承認済み」に変更する → `event-calendar-page.html`のカレンダーとトップページのミニカレンダーに自動で表示される
6. **お知らせ**：「お知らせ」シートに1行追加し、「公開」列に「公開」と入力する → トップページのお知らせ欄に自動で表示される
   （「お知らせ」シートが無い場合は、Apps Scriptエディタで`initAnnouncementSheet`関数を一度実行するか、初回のサイトアクセス時に自動作成されます）

詳しい入力例は`CONTENT-UPDATE-GUIDE.md`を参照してください。HTMLを直接編集する必要があるのは「ガイドラインページの文言修正」のみです。

---

## 今後の拡張候補（余裕があれば）

- 管理者用の簡易ステータス管理画面（GAS + HtmlService、または別途Webアプリ）
- イベント種別（公認大会／交流会／初心者体験会／観戦会／配信イベント）ごとに、カレンダーの色分けを増やす（現状は2色に簡略化）
